package ke.co.smartroundclinic.common
// Permission enum removed — permissions are now route-derived strings (e.g. "GET:/admin/kmpdc/{id}")
// The full catalog is auto-synced to MongoDB on startup via PermissionCatalogSync.
